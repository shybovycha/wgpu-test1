# WGPU tests

This repo contains my test&trial runs at WGPU.

## Building & running

To build each sample, run from within sample directory:

```bash
$ cargo build
```

To run a sample, run:

```bash
$ cargo run
```
